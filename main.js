separateWords = (str) => {
    let words = str.split(" ");
    console.log("There are " + words.length + " words.");
}

separateWords("I love vanilla JavaScript and arrow functions so much!"); // There are 9 words.